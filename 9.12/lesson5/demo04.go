package main

func main() {

	//var strSlice = []string{"1", "2"}

	//for i := 0; i < len(strSlice); i++ {
	//	fmt.Println(strSlice[i])
	//}

	//for _, v := range strSlice {
	//	fmt.Println(v)
	//}

	//a := [5]int{1, 2, 3, 4, 5} //基于数组定义切片   也可以基于切片的切片
	//b := a[:]
	//fmt.Printf("%v %T", b, b) //[1 2 3 4 5] []int  这是一个切片  【】中没有数组大小

	//cap()可以获得切片的容量 len()获取切片的长度
	//看例子！！！
	//s := []int{1, 2, 3, 4, 5, 6}
	//b := s[1:3] //此时长度为 2  容量为5   这个操作也是对切片进行切片

}
